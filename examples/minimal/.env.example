# Copy to .env and adjust if needed.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/fleuve_minimal
NATS_URL=nats://localhost:4222
# Required by Fleuve for encrypted columns (use a long random value in production).
STORAGE_KEY=test-storage-key-32-characters-long!
