"""Workflows package for Sf2.

Each subfolder contains a separate workflow with its own:
- models.py: Events, Commands, and State
- workflow.py: Workflow implementation (decide + evolve)
- adapter.py: Side effects handler
"""
